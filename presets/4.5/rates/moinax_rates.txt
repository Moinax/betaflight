#$ TITLE: Moinax Rates Profiles
#$ FIRMWARE_VERSION: 4.3
#$ FIRMWARE_VERSION: 4.4
#$ FIRMWARE_VERSION: 4.5
#$ CATEGORY: RATES
#$ STATUS: COMMUNITY
#$ KEYWORDS: community, rates, profile
#$ AUTHOR: Moinax

#$ DESCRIPTION: Custom rate profiles for Style, Racing, and Cine flying styles. Select one or more profiles to apply.

#$ OPTION BEGIN (UNCHECKED): Style Rates (Profile 0)
    rateprofile 0
    set rates_type = ACTUAL
    set rateprofile_name = Style
    set roll_rc_rate = 18
    set pitch_rc_rate = 18
    set yaw_rc_rate = 18
    set roll_expo = 75
    set pitch_expo = 75
    set yaw_expo = 75
    set roll_srate = 100
    set pitch_srate = 100
    set yaw_srate = 100
#$ OPTION END

#$ OPTION BEGIN (UNCHECKED): Racing Rates (Profile 1)
    rateprofile 1
    set rates_type = ACTUAL
    set rateprofile_name = Racing
    set roll_rc_rate = 18
    set pitch_rc_rate = 18
    set yaw_rc_rate = 18
    set roll_expo = 75
    set pitch_expo = 75
    set yaw_expo = 75
    set roll_srate = 53
    set pitch_srate = 53
    set yaw_srate = 53
#$ OPTION END

#$ OPTION BEGIN (UNCHECKED): Cine Rates (Profile 2)
    rateprofile 2
    set rates_type = ACTUAL
    set rateprofile_name = Cine
    set thr_expo = 65
    set roll_rc_rate = 11
    set pitch_rc_rate = 11
    set yaw_rc_rate = 11
    set roll_expo = 75
    set pitch_expo = 75
    set yaw_expo = 75
    set roll_srate = 53
    set pitch_srate = 53
    set yaw_srate = 53
#$ OPTION END
